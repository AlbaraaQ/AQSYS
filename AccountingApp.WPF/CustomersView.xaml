<Window x:Class="AccountingApp.WPF.CustomersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Customers" Height="600" Width="1000"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        Background="Transparent" FlowDirection="RightToLeft">
    <Window.Resources>
        <Style TargetType="Window">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <Border CornerRadius="10" Background="#F0F2F5" BorderBrush="#CCCCCC" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="15" Opacity="0.3" Color="#000"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0" Height="40" CornerRadius="10,10,0,0" MouseDown="TitleBar_MouseDown">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#4A90E2" Offset="0"/>
                                            <GradientStop Color="#357ABD" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Grid>
                                        <TextBlock Text="{TemplateBinding Title}"
                                                   Foreground="White"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,0">
                                            <Button Content="&#xE921;" Style="{StaticResource WindowCloseButtonStyle}" Click="CloseButton_Click"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <ContentPresenter Grid.Row="1"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <RadioButton Content="عملاء" IsChecked="{Binding CustomerType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
            <RadioButton Content="موردين" IsChecked="{Binding CustomerType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}" Margin="10,0,0,0"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <TextBlock Text="الاسم"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Name, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="الاسم (انجليزي)"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.NameEN, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="رقم العميل"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.CustomerNo, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="الهاتف"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Telephone, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="الجوال"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Mobile, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="الفاكس"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Fax, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="البريد الالكتروني"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="العنوان"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Address, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="ملاحظات"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.Notes, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="الرقم الضريبي"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.TaxNo, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="حد الائتمان"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SelectedCustomer.CreditLimit, UpdateSourceTrigger=PropertyChanged}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="جديد" Style="{StaticResource PrimaryButton}" Command="{Binding NewCommand}"/>
                        <Button Content="حفظ" Style="{StaticResource SuccessButton}" Command="{Binding SaveCommand}" Margin="10,0,0,0"/>
                        <Button Content="حذف" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
            <DataGrid Grid.Column="1" Margin="10" Style="{StaticResource ModernDataGrid}"
                      ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                      RowStyle="{StaticResource ModernDataGridRow}"
                      CellStyle="{StaticResource ModernDataGridCell}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="المعرف" Binding="{Binding Id}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                    <DataGridTextColumn Header="رقم العميل" Binding="{Binding CustomerNo}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="الهاتف" Binding="{Binding Telephone}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="الجوال" Binding="{Binding Mobile}" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>

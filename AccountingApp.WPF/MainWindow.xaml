<Window x:Class="AccountingApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AccountingApp" Height="600" Width="1000"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        Background="Transparent" FlowDirection="RightToLeft">
    <Window.Resources>
        <Style TargetType="Window">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <Border CornerRadius="10" Background="#F0F2F5" BorderBrush="#CCCCCC" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="15" Opacity="0.3" Color="#000"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Title Bar -->
                                <Border Grid.Row="0" Height="40" CornerRadius="10,10,0,0" MouseDown="TitleBar_MouseDown">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#4A90E2" Offset="0"/>
                                            <GradientStop Color="#357ABD" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Grid>
                                        <TextBlock Text="{TemplateBinding Title}"
                                                   Foreground="White"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"/>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,0">
                                            <Button Content="&#xE921;" Style="{StaticResource WindowCloseButtonStyle}" Click="CloseButton_Click"/>
                                            <Button Content="&#xE923;" Style="{StaticResource WindowButtonStyle}" Click="MaximizeButton_Click"/>
                                            <Button Content="&#xE922;" Style="{StaticResource WindowButtonStyle}" Click="MinimizeButton_Click"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Window Content -->
                                <ContentPresenter Grid.Row="1"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl Style="{StaticResource ModernTabControl}">
            <TabItem Header="البيانات الأساسية">
                <StackPanel Orientation="Horizontal">
                    <Button Content="الوحدات" Style="{StaticResource PrimaryButton}" Command="{Binding NavigateToUnitsCommand}" Margin="10"/>
                    <Button Content="العملاء" Style="{StaticResource PrimaryButton}" Command="{Binding NavigateToCustomersCommand}" Margin="10"/>
                    <Button Content="الموظفين" Style="{StaticResource PrimaryButton}" Command="{Binding NavigateToEmployeesCommand}" Margin="10"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="العملاء والموردين"/>
            <TabItem Header="شئون الموظفين"/>
            <TabItem Header="مبيعات ومشتريات"/>
            <TabItem Header="سندات"/>
            <TabItem Header="الحسابات"/>
            <TabItem Header="المخازن"/>
        </TabControl>
    </Grid>
</Window>
